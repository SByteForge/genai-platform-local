# Identity: the CD job for rag-platform-api uses this ServiceAccount's token
# (via a kubeconfig scoped to it) instead of full cluster-admin access.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: rag-platform-api-deployer
  namespace: dev
